- defaults:
    link:
      relink: true

- link:
    ~/.bashrc: 
      path: bash/.bashrc
      force: true
    ~/.profile:
      path: bash/.profile
      force: true
    ~/.aliases:
      path: bash/.aliases
      force: true
    ~/.gitconfig: git/.gitconfig
    ~/.vim: vim/.vim
    ~/.inputrc: 
      path: inputrc
      force: true
    ~/.vimrc:
      relink: true
      path: vim/.vimrc
    ~/.config/:
      glob: true
      path: config/*
      force: true
